# kvcrawl

A collection of standalone systems projects living in a single repository.

This repository contains:

* **Simple KV Store over RPC** — a lightweight key-value store accessible through RPC.
* **Web Crawler** — a standalone crawler for fetching and processing web data.

Each project is independent and can be built, tested, and run separately.
